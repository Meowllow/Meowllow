# Meowllow for Kitty

## Installation

Copy the `[flavor].conf` file of te theme you want in the kitty config directory (usually `~/.config/kitty/`) and add the following line at the top of your `kitty.conf` file:
```
include [flavor].conf
```